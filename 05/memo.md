バイナリプログラム(実行可能プログラム)  
⇒命令メモリにシリアライズ  
⇒命令レジスタ（CPU）  
⇒マイクロコードに変換（制御ユニット）  
⇒次の命令をフェッチ  
I/Oデバイス-メモリマップドI/OでPCのメモリ領域に割り当て

- 命令メモリ（ROM）
  -  アドレス指定->中身を出力

- CPU
  - A命令のとき, 0ccccccccccccccc, c命令のとき, 1xxaccccccdddjjjであることに注意し実装した。
  - Aレジスタ⇒Dレジスタ⇒ALU⇒PC⇒loadPCの順に実装した。
  - 各bitが何を意味するのかを考えなければならず難しかった。
- Mmory
  - アドレスの上位bitから, メモリのどこの領域を指しているのかで区別。
  - address[14], [13]を見て判断する。00->RAM16K, 10->Screenなど。
  - 各出力をまとめる。
- Computer
  - 表に示された構造をそのまま実装した。 
